# Flask
SECRET_KEY=change-this-to-a-random-secret
FLASK_ENV=development

# Database — leave blank to use the default (backend/relay.db)
# Override with an absolute path only if you need a custom location:
# DATABASE_URI=sqlite:////absolute/path/to/your/relay.db

# JWT signing secret
JWT_SECRET=change-this-to-a-random-jwt-secret
